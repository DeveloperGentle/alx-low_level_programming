#include <stdlib.h>
#include <time.h>
#include <stdio.h>
 
/**
 * main - prints the alphabet in lower case
 * number stored in the variable n
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i;
	for(i = 'a';i<='z';i++) {
	putchar(i);

	}
	return (0);
}
